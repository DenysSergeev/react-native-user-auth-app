import { StyleSheet } from 'react-native';

const dashboarsStyles = StyleSheet.create({
  welcomeText: {
    fontSize: 16,
    paddingHorizontal: 20,
    paddingBottom: 40,
  },
  logoutContainer: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    paddingHorizontal: 20,
    paddingBottom: 40,
  },
  logoutText: {
    color: '#1F41BB',
    fontSize: 24,
  },
});

export default dashboarsStyles;
